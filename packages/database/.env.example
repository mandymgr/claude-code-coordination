# Neon Database URLs
# Get these from your Neon dashboard: https://console.neon.tech
DATABASE_URL="postgresql://username:password@hostname/database?sslmode=require"
DIRECT_URL="postgresql://username:password@hostname/database?sslmode=require"

# Development
NODE_ENV="development"

# Optional: Shadow database for migrations (recommended for production)
SHADOW_DATABASE_URL="postgresql://username:password@hostname/shadow_database?sslmode=require"